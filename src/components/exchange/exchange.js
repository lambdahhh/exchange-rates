import React from 'react';
import './exchange.css';

const Exchange = () => {
    return (
        <div className="exchange d-flex">
            Exchange
        </div>
    );
};

export default Exchange;